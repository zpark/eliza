import type { Plugin } from '@elizaos/core';

class position {
  // wallet (strategy)
  // chain / token
  // buy price (in base token) usd price at the time
  // amount (target/actual received, how slippage)
  // initial exist target price
  // reasoning
  // sentiment/scoring at the time (point to token history) => bought at date/time
  // strategy settings at the time
  // block
}
